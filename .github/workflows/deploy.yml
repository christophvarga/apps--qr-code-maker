name: Deploy to Edge

# pre->main merge handled by Woodpecker CI (.woodpecker/auto-merge.yml)
# This workflow only triggers edge deployment on main

on:
  push:
    branches:
      - main  # Only main - pre is handled by Woodpecker auto-merge
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'prod'
        type: choice
        options:
          - prod
          - preview

env:
  INFRA_REPO: christophvarga/infra--ci-cd

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Set environment
        id: env
        run: |
          if [ "${{ inputs.environment }}" != "" ]; then
            echo "environment=${{ inputs.environment }}" >> $GITHUB_OUTPUT
          else
            echo "environment=prod" >> $GITHUB_OUTPUT
          fi

      - name: Trigger Edge Deployment
        env:
          GITHUB_TOKEN: ${{ secrets.INFRA_DEPLOY_TOKEN }}
        run: |
          curl -X POST \
            -H "Authorization: token $GITHUB_TOKEN" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/repos/$INFRA_REPO/dispatches" \
            -d '{
              "event_type": "edge-deploy",
              "client_payload": {
                "app_name": "qr",
                "environment": "${{ steps.env.outputs.environment }}",
                "branch": "main"
              }
            }'
